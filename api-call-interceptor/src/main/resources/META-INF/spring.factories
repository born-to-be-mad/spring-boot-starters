# Spring Boot searches through all spring.factories file it finds on the classpath and load the configurations declared within
org.springframework.boot.autoconfigure.EnableAutoConfiguration=\
  by.dma.apicallinterceptor.starter.ApiCallAutoConfiguration
